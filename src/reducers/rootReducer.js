const schedulingInfo = {
    schedulingOn: false
};

const reducer = (state = schedulingInfo, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  
  export default reducer;